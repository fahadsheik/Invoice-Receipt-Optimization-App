export interface Invoice {
  id: string;
  invoiceNumber: string;
  vendorName: string;
  poNumber: string;
  amount: string;
  date: string;
  dueDate: string;
  status: string;
  createdAt: string;
}